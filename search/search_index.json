{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Fluent JDBC Client for Jakarta EE/CDI","text":"<p>Fluent JDBC Client is a lightweight, framework-agnostic JDBC client for Jakarta EE and CDI applications. It provides the fluent developer experience of Spring's <code>JdbcClient</code> without introducing a Spring dependency.</p> <p>The project is organized into three core modules:</p> Module Artifact Description <code>core</code> <code>jdbc-client-core</code> Contains the plain <code>JdbcClient</code> API and helper types. It depends only on the JDK's <code>javax.sql.DataSource</code>. <code>config</code> <code>jdbc-client-config</code> Integrates with MicroProfile Config and exposes a configurable <code>JdbcConfig</code> bean. <code>cdi</code> <code>jdbc-client-cdi</code> Provides CDI producers for <code>JdbcClient</code> and the CDI-discovered <code>ConverterRegistry</code>. <p>The repository also includes Arquillian integration tests for GlassFish and WildFly. These examples provide a practical reference for validating Jakarta EE/CDI applications in real runtimes.</p> <p>Follow this reading order:</p> <ul> <li>Start with installation to select the appropriate dependency.</li> <li>Continue with quickstart for the fastest path to using the client.</li> <li>Explore querying and result mapping and updates for detailed behavior.</li> <li>Review configuration for placeholders, timeouts, and defaults.</li> </ul>"},{"location":"configuration/","title":"Configuration","text":"<p>Configuration defines the default behavior for SQL placeholder rewriting, query timeouts, and fetch-size hints. These defaults keep database access consistent throughout an application while still allowing individual statements to override behavior when needed.</p>"},{"location":"configuration/#programmatic-configuration","title":"Programmatic configuration","text":"<p>The <code>core</code> module provides a <code>JdbcConfig</code> class for setting the default SQL placeholder, query timeout, and fetch size.</p> <p>A <code>JdbcConfig</code> instance can be created programmatically:</p> <pre><code>JdbcConfig config = new JdbcConfig(...);\n</code></pre> <p>Then apply it when constructing a <code>JdbcClient</code>:</p> <pre><code>JdbcClient client = new JdbcClient(dataSource, converters, config);\n</code></pre> <p>The builder API flattens these settings into fine-grained methods:</p> <pre><code>JdbcClient client = JdbcClient.builder(dataSource)\n        .placeholder(\"$\")\n        .queryTimeout(30)\n        .fetchSize(100)\n        .build();\n</code></pre>"},{"location":"configuration/#declarative-configuration-in-jakarta-eecdi","title":"Declarative configuration in Jakarta EE/CDI","text":"<p>In a Jakarta EE/CDI environment, the optional <code>jdbc-client-config</code> module can populate this configuration from MicroProfile Config properties.</p> Property Default Description <code>jdbcclient.placeholder</code> <code>?</code> Placeholder used when rewriting named parameters such as <code>:name</code>. <code>jdbcclient.query-timeout</code> <code>0</code> Default query timeout in seconds. <code>0</code> means no timeout. <code>jdbcclient.fetch-size</code> <code>0</code> Default fetch-size hint. <code>0</code> uses the driver default. <p>Named parameters are rewritten according to the configured placeholder:</p> Placeholder Rewritten SQL Typical database <code>?</code> <code>?</code> MySQL and standard JDBC <code>$</code> <code>$1</code>, <code>$2</code>, ... H2/PostgreSQL <code>:</code> <code>:1</code>, <code>:2</code>, ... Oracle <code>@</code> <code>@name</code> SQL Server <p>When <code>jdbcclient.placeholder=$</code>, the following SQL:</p> <pre><code>SELECT id FROM engineers WHERE id = :id AND active = :active\n</code></pre> <p>is rewritten before it is sent to the driver as:</p> <pre><code>SELECT id FROM engineers WHERE id = $1 AND active = $2\n</code></pre>"},{"location":"configuration/#overriding-configuration-for-a-query","title":"Overriding configuration for a query","text":"<p>The <code>queryTimeout(...)</code> and <code>fetchSize(...)</code> methods on an individual SQL specification override the client defaults for that statement.</p> <pre><code>List&lt;DevSummary&gt; devs = client\n        .sql(\"SELECT id, dev_name FROM engineers\")\n        .fetchSize(100)\n        .queryTimeout(30)\n        .query(DevSummary.class)\n        .list();\n</code></pre> <p>In practice, these defaults are useful for keeping database behavior consistent across an application while still allowing particular queries to opt into stricter or more permissive settings when needed. The same pattern applies whether the client is configured programmatically or through MicroProfile Config in a Jakarta EE environment.</p> <p>For query execution and mapping behavior, continue with querying and result mapping.</p>"},{"location":"install/","title":"Installation","text":"<p>Choose the dependency that matches your runtime. The core module is intended for Java SE applications, while the CDI and configuration integrations are for Jakarta EE-compatible runtimes.</p>"},{"location":"install/#requirements","title":"Requirements","text":"<ul> <li>JDK 21 or later.</li> <li>A Jakarta EE 11 / CDI-compatible runtime is required for the <code>cdi</code> and <code>config</code> modules, such as GlassFish 8 or WildFly 41.</li> <li>The <code>core</code> module is usable in plain Java SE applications because it depends only on <code>javax.sql.DataSource</code>.</li> </ul>"},{"location":"install/#using-the-core-jdbcclient","title":"Using the core <code>JdbcClient</code>","text":"<p>Use the following dependency when only the core fluent <code>JdbcClient</code> API is required:</p> <pre><code>&lt;dependency&gt;\n    &lt;groupId&gt;io.github.hantsy.jdbc&lt;/groupId&gt;\n    &lt;artifactId&gt;jdbc-client-core&lt;/artifactId&gt;\n    &lt;version&gt;1.0.0-SNAPSHOT&lt;/version&gt;\n&lt;/dependency&gt;\n</code></pre> <p>The core module has no runtime dependency beyond the JDK. It expects a <code>DataSource</code> supplied by the application or runtime environment.</p>"},{"location":"install/#integrating-with-cdi","title":"Integrating with CDI","text":"<p>For a CDI-managed <code>JdbcClient</code>, add the CDI artifact:</p> <pre><code>&lt;dependency&gt;\n    &lt;groupId&gt;io.github.hantsy.jdbc&lt;/groupId&gt;\n    &lt;artifactId&gt;jdbc-client-cdi&lt;/artifactId&gt;\n    &lt;version&gt;1.0.0-SNAPSHOT&lt;/version&gt;\n&lt;/dependency&gt;\n</code></pre> <p>This artifact depends on the core module and is intended for a Jakarta EE 11 runtime that provides the Jakarta EE APIs.</p>"},{"location":"install/#integrating-with-microprofile-config","title":"Integrating with MicroProfile Config","text":"<p>To configure the library from MicroProfile Config properties, add the optional configuration module:</p> <pre><code>&lt;dependency&gt;\n    &lt;groupId&gt;io.github.hantsy.jdbc&lt;/groupId&gt;\n    &lt;artifactId&gt;jdbc-client-config&lt;/artifactId&gt;\n    &lt;version&gt;1.0.0-SNAPSHOT&lt;/version&gt;\n&lt;/dependency&gt;\n</code></pre> <p>All modules are published with the same version. Replace the snapshot version shown above with the release version used by the application.</p> <p>After adding the appropriate module, start using the fluent API for executing queries and updates. See quickstart for the shortest path to using the client, and querying and result mapping for more detailed behavior.</p>"},{"location":"querying/","title":"Querying and result mapping","text":"<p><code>JdbcClient.sql(...)</code> returns a SQL specification. It supports named parameters such as <code>:name</code>, positional parameters such as <code>?</code>, statement hints, typed mapping, and update statements.</p>"},{"location":"querying/#retrieving-simple-values","title":"Retrieving simple values","text":"<p>Use <code>singleValue(...)</code> or <code>optionalValue(...)</code> to read a single scalar result from the database.</p> <pre><code>Long count = client.sql(\"SELECT COUNT(*) FROM engineers\")\n        .singleValue(Long.class);\n\nOptional&lt;Long&gt; maybeCount = client.sql(\"SELECT COUNT(*) FROM engineers\")\n        .optionalValue(Long.class);\n</code></pre>"},{"location":"querying/#mapping-results-to-records-and-pojos","title":"Mapping results to records and POJOs","text":"<p>The built-in typed mapper matches result-column labels to record components or fields. Underscores are ignored when matching a column name to a record component.</p> <pre><code>public record DevSummary(Long id, String devName) {\n}\n\nList&lt;DevSummary&gt; devs = client\n        .sql(\"SELECT id, dev_name FROM engineers ORDER BY id\")\n        .query(DevSummary.class)\n        .list();\n</code></pre> <p>The mapper also works with a no-argument POJO whose fields match the result columns.</p> <p>The following result-access patterns are available:</p> <ul> <li><code>single()</code> requires exactly one row.</li> <li><code>optional()</code> allows zero or one row.</li> <li><code>list()</code> reads all rows before returning.</li> <li><code>stream()</code> returns a lazy stream, and the stream should be closed when work is complete so JDBC resources are released.</li> </ul> <pre><code>DevSummary one = client.sql(\"SELECT ... WHERE id = :id\")\n        .param(\"id\", 1L)\n        .query(DevSummary.class)\n        .single();\n\nOptional&lt;DevSummary&gt; maybeOne = client.sql(\"SELECT ... WHERE id = :id\")\n        .param(\"id\", 1L)\n        .query(DevSummary.class)\n        .optional();\n\nStream&lt;DevSummary&gt; stream = client.sql(\"SELECT ...\")\n        .query(DevSummary.class)\n        .stream();\n</code></pre>"},{"location":"querying/#customizing-row-mapping","title":"Customizing row mapping","text":"<p>Mapping can be defined inline with a lambda or reused through a dedicated <code>RowMapper</code>.</p> <pre><code>List&lt;String&gt; names = client\n        .sql(\"SELECT dev_name FROM engineers ORDER BY id\")\n        .query((rs, rowNum) -&gt; rs.getString(\"dev_name\"))\n        .list();\n\nJdbcClient.RowMapper&lt;DevSummary&gt; mapper = (rs, rowNum) -&gt;\n        new DevSummary(rs.getLong(\"id\"), rs.getString(\"dev_name\"));\n\nList&lt;DevSummary&gt; devs = client\n        .sql(\"SELECT id, dev_name FROM engineers\")\n        .query(mapper)\n        .list();\n</code></pre>"},{"location":"querying/#configuring-converters","title":"Configuring converters","text":"<p>Converters translate a database value into the Java type requested by a typed mapper or scalar query. The built-in mapper supports common conversions for numeric types, strings, booleans, SQL date/time types, and Java time types.</p> <p>Use a <code>ConverterRegistry</code> with a standalone client:</p> <pre><code>ConverterRegistry registry = new ConverterRegistry()\n        .register(UUID.class, String.class, UUID::toString)\n        .register(String.class, UUID.class, UUID::fromString);\n\nJdbcClient client = JdbcClient.builder(dataSource)\n        .converters(registry)\n        .build();\n</code></pre> <p>A converter implements <code>Converter&lt;S, T&gt;</code>:</p> <pre><code>Converter&lt;UUID, String&gt; converter = UUID::toString;\nregistry.register(UUID.class, String.class, converter);\n</code></pre> <p>In a CDI application, register the converter as an application-scoped bean. The CDI integration automatically discovers the generic source and target types.</p> <pre><code>@ApplicationScoped\npublic class UuidToStringConverter implements Converter&lt;UUID, String&gt; {\n\n    @Override\n    public String convert(UUID source) {\n        return source.toString();\n    }\n}\n</code></pre> <p>Converters are consulted when the JDBC value is not already assignable to the requested target type.</p> <p>For executing inserts, updates, and batch statements, see updates. For configuration options, see configuration.</p>"},{"location":"quickstart/","title":"Quickstart","text":"<p>The fastest path to using the library is to construct a <code>JdbcClient</code> from a <code>DataSource</code> and start composing fluent SQL with strongly typed results.</p>"},{"location":"quickstart/#creating-jdbcclient","title":"Creating <code>JdbcClient</code>","text":"<p>After <code>jdbc-client-core</code> has been added to the project dependencies, a <code>JdbcClient</code> can be created directly from a <code>DataSource</code>:</p> <pre><code>import io.github.hantsy.jdbc.JdbcClient;\nimport javax.sql.DataSource;\n\nDataSource dataSource = ...; // obtain a DataSource from the application or runtime\n\nJdbcClient client = new JdbcClient(dataSource);\n</code></pre> <p>The builder API also supports explicit configuration:</p> <pre><code>JdbcClient client = JdbcClient.builder(dataSource)\n        .placeholder(\"?\")\n        .queryTimeout(30)\n        .fetchSize(100)\n        .converters(registry)\n        .build();\n</code></pre> <p>See configuration for the full list of available defaults and options.</p> <p>In a Jakarta EE/CDI environment, CDI can create and inject the client. Add <code>jdbc-client-cdi</code> and <code>jdbc-client-config</code>, then expose a <code>DataSource</code> as a bean:</p> <pre><code>@ApplicationScoped\npublic class TestDataSourceProducer {\n\n    @Resource(lookup = \"java:comp/MyDS\")\n    private DataSource dataSource;\n\n    @Produces\n    @ApplicationScoped\n    public DataSource expose() {\n        return dataSource;\n    }\n}\n</code></pre> <p>The <code>JdbcClient</code> is then available for injection:</p> <pre><code>@Inject JdbcClient client;\n</code></pre>"},{"location":"quickstart/#executing-queries","title":"Executing queries","text":"<p>Once the client is available, the fluent SQL API can be used for both reading and writing data. The library supports both named and positional parameters in a single SQL statement.</p> <p>Use named parameters with <code>param(...)</code>:</p> <pre><code>List&lt;DevSummary&gt; devs = client\n        .sql(\"SELECT id, dev_name FROM engineers WHERE id = :id\")\n        .param(\"id\", 1L)\n        .query(DevSummary.class)\n        .list();\n</code></pre> <p>The client also accepts positional parameters:</p> <pre><code>List&lt;DevSummary&gt; devs = client\n        .sql(\"SELECT id, dev_name FROM engineers WHERE id = ?\")\n        .param(1L)\n        .query(DevSummary.class)\n        .list();\n</code></pre> <p>A single SQL specification cannot combine named and positional parameters.</p> <p>See querying and mapping for more examples of result mapping, row mappers, and streaming.</p>"},{"location":"updates/","title":"Updates, generated keys, and batches","text":"<p><code>JdbcClient</code> supports write operations, generated keys, and efficient batch updates without leaving the fluent API. The examples below cover the common patterns used when modifying data in an application.</p>"},{"location":"updates/#updating-data","title":"Updating data","text":"<p>Use <code>update()</code> for SQL statements such as <code>INSERT</code>, <code>UPDATE</code>, and <code>DELETE</code>. The method returns the number of rows affected.</p> <pre><code>int rows = client\n        .sql(\"UPDATE engineers SET dev_name = :name WHERE id = :id\")\n        .param(\"name\", \"Ada\")\n        .param(\"id\", 1L)\n        .update();\n</code></pre>"},{"location":"updates/#handling-generated-keys","title":"Handling generated keys","text":"<p>When inserting a row and generated keys are required, pass a <code>GeneratedKeyHolder</code> to the update call.</p> <pre><code>GeneratedKeyHolder keys = new GeneratedKeyHolder();\n\nint rows = client\n        .sql(\"INSERT INTO engineers (dev_name) VALUES (:name)\")\n        .param(\"name\", \"Ada\")\n        .update(keys);\n\nObject generatedKey = keys.getKey();\nMap&lt;String, Object&gt; firstRow = keys.getKeys();\nList&lt;Map&lt;String, Object&gt;&gt; allRows = keys.getKeyList();\n</code></pre> <p>The JDBC driver must support generated keys. <code>getKey()</code> returns the first column from the first generated-key row, while <code>getKeys()</code> and <code>getKeyList()</code> preserve column labels and multiple rows.</p>"},{"location":"updates/#executing-batch-updates","title":"Executing batch updates","text":"<p>Use a list of named-argument maps for batch updates:</p> <pre><code>int[] results = client\n        .sql(\"UPDATE engineers SET active = :active WHERE id = :id\")\n        .batchUpdate(List.of(\n                Map.of(\"active\", true, \"id\", 1L),\n                Map.of(\"active\", false, \"id\", 2L)\n        ));\n</code></pre> <p>For positional SQL, pass an array for each batch entry:</p> <pre><code>int[] results = client\n        .sql(\"UPDATE engineers SET active = ? WHERE id = ?\")\n        .batchUpdate(new Object[][] {\n                {true, 1L},\n                {false, 2L}\n        });\n</code></pre> <p>These patterns keep write operations compact and consistent while exposing the database-specific capabilities needed for generated keys and bulk updates.</p> <p>For query execution, parameter binding, and result mapping, see querying and result mapping.</p>"}]}