package io.github.hantsy.jdbc;

import javax.sql.DataSource;
import jakarta.enterprise.context.ApplicationScoped;
import jakarta.enterprise.context.Initialized;
import jakarta.enterprise.event.Observes;
import jakarta.inject.Inject;

/**
 * Creates the {@code engineers} table once the application scope is initialized.
 */
@ApplicationScoped
public class DatabaseInitializer {

    @Inject
    private DataSource dataSource;

    public void init(@Observes @Initialized(ApplicationScoped.class) Object event) throws Exception {
        try (var conn = dataSource.getConnection();
             var stmt = conn.createStatement()) {
            stmt.execute("CREATE TABLE IF NOT EXISTS engineers ("
                    + "id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, dev_name VARCHAR(255))");
        }
    }
}
